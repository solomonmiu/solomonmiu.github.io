describe(" find the longest word in the array", function () {
    it("find the longest word in the array", function () {
    assert.equal(findLongestWord(["hi","bye","hello","greetings"]),"greetings");
   })
});