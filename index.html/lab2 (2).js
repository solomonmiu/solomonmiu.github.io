"use strict";
/* global assert isVowel*/
/* isVowel() that takes a character (i.e. a string of length 1) and returns true if it is a vowel, false
otherwise. */
describe(" sum of the element in the array", function () {
 it("sumArray", function () {
 assert.equal(sumArray([2,7,4]), 13);
}
)});

describe("sum of the element in the array", function () {
    it("sumarray", function () {
    assert.equal(sumArray([2,5]), 7);
   })
});
